// Copy this file to "config.js" and fill in your own ipify API key.
// Get a free key at: https://geo.ipify.org/
//
// config.js is listed in .gitignore and will NOT be committed —
// this file (config.example.js) is the one that stays public,
// so anyone cloning the repo knows what to fill in.

const CONFIG = {
  API_KEY: "YOUR_IPIFY_API_KEY_HERE",
};